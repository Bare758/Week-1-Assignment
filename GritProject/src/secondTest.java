
public class secondTest {
	
		// Assignment 1
		public long returnLowestLong(long x, long y) {
			return Math.min(x, y);
		}
		// Assignment 2
		public float returnLowestFloat(float x, float y) {
			return Math.min(x, y);
		}
		// Assignment 3
		public int returnLowestInt(int x, int y) {
			return Math.min(x, y);
		}
		// Assignment 4
		public double returnLowestDouble(double x, double y) {
			return Math.min(x, y);
		}
		// Assignment 5 & 7
		public int returnMaxInt(int x, int y) {
			return Math.max(x, y);
		}
		// Assignment 6
		public double returnMaxDouble(double x, double y) {
			return Math.max(x, y);
		}
		// Assignment 8
		public long returnMaxLong(long x, long y) {
			return Math.max(x, y);
		}
		// Assignment 9
		public double returnPow(double x, double y) {
			return Math.pow(x, y);
		}
		// Assignment 10
		public int returnPlusOneInt(int i) {
			if(i < Integer.MAX_VALUE) ++i;
			return i;
		}
		// Assignment 11
		public long returnPlusOneLong(long i) {
			if(i < Long.MAX_VALUE) ++i;
			return i;
		}
		// Assignment 12
		public int returnMinOneInt(int i) {
			if(i > Integer.MIN_VALUE) --i;
			return i;
		}
		// Assignment 13
		public long returnMinOneLong(long i) {
			if(i > Long.MIN_VALUE) --i;
			return i;
		}
		// Assignment 14
		public double returnSquareRootDouble(double d) {
			return Math.sqrt(d);
		}
		// Assignment 15
		public double returnCubeRootDouble(double d) {
			return Math.cbrt(d);
		}
		// Bonus Assignment
		public double returnRandomDouble() {
			return Math.random();
		}
	
		
		
	}



